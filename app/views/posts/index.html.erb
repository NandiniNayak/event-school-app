<p id="notice"><%= notice %></p>

<h1>Listing Posts</h1>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Category</th>
      <th>Year group</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
    
            <td><%= post.title %></td>
        <td><%= post.description %></td>
        <td><%= post.category %></td>
        <td><%= post.year_group.name %></td>
        </div>
        <td class ="button"><%= link_to 'Show', post %></td>
        <td class ="button"><%= link_to 'Edit', edit_post_path(post) %></td>
        <td class ="button"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div class ="row">
    <div class="col-md-8">
    </div>
</div>
<td><%= link_to 'New Post',new_post_path, class:  'btn btn-success btn-xs' %></td>
